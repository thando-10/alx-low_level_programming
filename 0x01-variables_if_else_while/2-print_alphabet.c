#include <stdio.h>

/**
 * main - this is the main function that will carry all the instructions
 *
 * Return: this will return the value 0
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
