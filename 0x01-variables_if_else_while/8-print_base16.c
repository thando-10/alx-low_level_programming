#include <stdio.h>

/**
 * main - this function will carry the instructions to be executed
 *
 * Return: this will return the value 0
 */

int main(void)
{
	int i;
	char j;

	for (i = 0 ; i < 10 ; i++)
		putchar(i + '0');
	for (j = 'a' ; j <= 'f' ; j++)
		putchar(j);
	putchar('\n');
	return (0);
}
