#include "main.h"

/**
 * print_alphabet - this function to print in lower case
 * Return: this will return the value 0
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		_putchar(i);
	_putchar('\n');
}
